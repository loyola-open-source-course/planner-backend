server:
  port: 8080

spring:
  application:
    name: Planner
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/planner?useUnicode=true&characterEncoding=UTF-8&useSSL=false&useLegacyDatetimeCode=false&useJDBCCompliantTimezoneShift=true&serverTimezone=UTC
    username: root
    password: 111
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update